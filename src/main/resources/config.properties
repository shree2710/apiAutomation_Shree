# config for UI and API
reqres.baseUrl=https://reqres.in/api/users?page=2
reqres.apiKey=reqres_1f66c01c2e0d493ca22fef8494ef8d21

# ---- custom auth server ----
auth.baseUrl=http://64.227.160.186:8080

# ---- petstore (CRUD) ----
petstore.baseUrl=https://petstore.swagger.io/v2

# ---- local json-server (POST/schema demos) ----
jsonserver.baseUrl=http://localhost:3000

# ---- UI automation (SauceDemo) ----
ui.baseUrl=https://www.saucedemo.com
ui.browser=chrome
ui.headless=true
ui.username=standard_user
ui.password=secret_sauce

# ---- execution / retry ----
retry.count=1

# kept for backward-compatibility with older tests
baseUrl=https://reqres.in/api/users?page=2
